name: Build and deploy
on:
  push:
    branches:
      - master

jobs:
  main_job:
    runs-on: ubuntu-latest
    steps:
      - run: curl -L https://fly.io/install.sh | sh
      - run: ~/.fly/bin/flyctl deploy -t {{ secrets.FLY_TOKEN }}
